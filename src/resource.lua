
resources = {}

function resources:init()
	-- local imageData = love.image.newImageData("myImage.png")

	-- local myPicture = love.image.newImageData( imageData:getWidth() / 16, imageData:getHeight() )

	-- for x = 0, myPicture:getWidth()-1 do
	-- 	for y = 0, myPicture:getHeight()-1 do
	-- 		local r, g, b, a = imageData:getPixel(x, y)
	-- 		myPicture:setPixel(x, y, r, g, b, a)
	-- 	end
	-- end

	-- resources.myImage = love.graphics.newImage(myPicture)

end